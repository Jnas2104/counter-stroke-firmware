// Linker Taster = Pfeil links, rechter Taster = Pfeil rechts – die Standardtasten der App.
#include <behaviors.dtsi>
#include <dt-bindings/zmk/keys.h>

/ {
    keymap {
        compatible = "zmk,keymap";

        default_layer {
            bindings = <&kp LEFT &kp RIGHT>;
        };
    };
};
